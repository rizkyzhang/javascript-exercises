/*
  Title:
    A random integer from min to max

  Description:
    Create a function getRandomInt(min, max) that generates a random integer number from min to max including both min and max as possible values.

  Example:
    console.log(getRandomInt(2, 10)); 
    // -> 4.56

  Link:
    http://javascript.info/task/random-int-min-max
*/
