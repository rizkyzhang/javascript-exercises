/*
  Title:
    Check whether an input is an array or not

  Description:
    Write a JavaScript function to check whether an input is an array or not.

  Examples:
    console.log(isArray(2));
    // -> false

    console.log(isArray({}));
    // -> false

    console.log(isArray([]));
    // -> true

  Note:
    In JavaScript, arrays are not true arrays.
    They are actually objects.
    So you can’t simply do a typeof check.
    Because it will return object.

  Link:
    https://www.w3resource.com/javascript-exercises/javascript-array-exercise-1.php
*/

