/*
  Description:
    Given two strings, create a function that return true if the first string contains all of the letters of the second string.
  
  Examples:
    console.log(compareStrings("hello", "Hello"));
    // -> true // because all of the letters in the second string are present in the first, ignoring case.
  
    console.log(compareStrings("hello", "hey"));
    // -> false // because the string "hello" does not contain a "y".

    console.log(compareStrings("Alien", "line"));
    // -> true // because all of the letters in "line" are present in "Alien".
*/

function compareStrings(string1, string2) {

}

module.exports = compareStrings;

