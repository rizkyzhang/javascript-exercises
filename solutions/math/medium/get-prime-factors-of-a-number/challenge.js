/*
  Title:
    Get prime factors of a number

  Description:
    Create a function to get prime factors of a number

  Bonus:
    1. Add one more parameter, minOrMax with default value of null,
       if user pass min to it, return the smallest prime factor
       and vice versa.

  Examples:
    console.log(primeFactors(100));
    // -> 2 * 2 * 5 * 5

    console.log(primeFactors(-120));
    // -> -120 is not a positive integer

    console.log(primeFactors(80.5));
    // -> 80.5 is not an integer
*/

